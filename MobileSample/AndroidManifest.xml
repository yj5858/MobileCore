<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobile.sample"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Network State Permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="15" />
<!--   yangjia            use MobileCore library to handle the exception -->
    <application
        android:name="com.mobile.core.exception.CustomApplication"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.mobile.sample.activity.MainActivity"
            android:label="@string/title_activity_main"
            
            android:theme="@android:style/Theme.Holo.NoActionBar"
            android:windowSoftInputMode="stateUnspecified|adjustPan" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
      
      

        <activity
            android:name="com.mobile.sample.activity.NextActivity"
            android:label="@string/title_activity_next" >
        </activity>
    </application>

</manifest>